package section10StreamsAndLambdas.firstStepsIntoStreamsAPI161;

interface IEmployee {  // List employees qu'on essaye de trié est de type IEmployee => extends interface Comparable (pas implemente car IEmployee est une interface)
        int getSalary();
        }
